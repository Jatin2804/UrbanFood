// Default English translations (fallback)
// Loaded from JSON file for consistency

import defaultTranslationsJson from './translations.json';

export const defaultTranslations = defaultTranslationsJson;
